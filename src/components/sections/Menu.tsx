import {FC} from 'react'
import {Section} from '../layout/Section'
import {Nav} from '../Nav'

const Menu: FC = () => {

  return (
    <Section marginBottom={62}>
      <Nav />
    </Section>
  )
}

export { Menu }
