import {FC} from 'react'
import {Section} from '../layout/Section'
import {Title} from '../Title'

const Features: FC = () => {

  return (
    <Section id={'features'}>
      <Title title={'Features'} />
    </Section>
  )
}

export { Features }
